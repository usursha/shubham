server:
  port: 9200

springdoc:
 api-docs:
   path: /v3/api-docs #these urls will used to load the swagger ui of this service by the api gateway
 swagger-ui:
   path: /swagger-ui.html
#spring:
#  kafka:
#    bootstrap-servers: localhost:9092
#    producer:
#      key-serializer: org.apache.kafka.common.serialization.StringSerializer
#      value-serializer: org.springframework.kafka.support.serializer.JsonSerializer
#    consumer:
#      group-id: api-logs-group
#      auto-offset-reset: earliest
#      key-deserializer: org.apache.kafka.common.serialization.StringDeserializer
#      value-deserializer: org.springframework.kafka.support.serializer.ErrorHandlingDeserializer
#      properties:
#        spring:
#          deserializer:
#            value:
#              delegate:
#                class: org.springframework.kafka.support.serializer.JsonDeserializer
#          json:
#            trusted:
#              packages: "*"
spring:
  kafka:
    bootstrap-servers: ${SPRING_KAFKA_BOOTSTRAP_SERVERS:localhost:9094}
    producer:
      key-serializer: org.apache.kafka.common.serialization.StringSerializer
      value-serializer: org.apache.kafka.common.serialization.StringSerializer
      acks: all
      retries: 3
      batch-size: 16384
      buffer-memory: 33554432
      properties:
        max.in.flight.requests.per.connection: 1
        enable.idempotence: true
  aop:
    proxy-target-class: false
    auto: true
  boot:
    admin:
      client:
         url: http://localhost:8000 #Admin portal url.
         instance:
           name: dashboardService
           service-base-url: http://localhost:9200 #This is current application url.         
  application:
    name: dashboard-service
  datasource:
    url: jdbc:sqlserver://172.16.15.120:1433;databaseName=OPS360;encrypt=true;trustServerCertificate=true;
    username: appuser
    password: hitachi@123
    driver-class-name: com.microsoft.sqlserver.jdbc.SQLServerDriver
  jpa:
    hibernate:
      ddl-auto: none

    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.SQLServerDialect
        format_sql: true

        
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: http://${keycloak.domain}/realms/${keycloak.realm}
          jwk-set-uri: ${spring.security.oauth2.resourceserver.jwt.issuer-uri}/protocol/openid-connect/certs
 
management:
  endpoints:
    web:
      exposure:
        include: '*'
  endpoint:
    health:
      show-details: always
          
jwt:
  auth:
    converter:
      resource-id: ${keycloak.resource}
      principle-attribute: preferred_username  
      
  
keycloak:
  domain: localhost:8080
  realm: HPY
  resource: ops-360
  issuer-uri: http://${keycloak.domain}
  adminClientSecret: 13ptMbHcKqskC5zC586keD7VNBGsUTkJ 
  auth-server-url: http://${keycloak.domain}
  adminClientId: ${keycloak.resource}
  urls:
    auth: http://${keycloak.domain}
    tokenURL: http://${keycloak.domain}/realms/${keycloak.realm}/protocol/openid-connect/token
  ssl-required: external
  use-resource-role-mappings: true
        
eureka:
  instance:
    prerfer-ip-address: true
  client:
    fetch-registry: true
    registry-with-eureka: true
    service-url:
#      defaultZone: http://172.16.15.192:9101/eureka
     defaultZone: http://127.0.0.1:9101/eureka
     
 
  
logging:
  level:
    com.hpy: DEBUG
  file:
    name: D:/var/logs/dashboard.log
     
synergy:
#  base-url: https://ops360dev.hitachi-payments.com/synergy/HitachiAPI_IntegrationUAT/api
  base-url: https://ops360dev.hitachi-payments.com/synergy/HitachiAPI_Integration/api
  username: OPS360API
  password: Cogent@123
  
app:
  cross-origin:
    allow: https://ops360uat.hitachi-payments.com
ops360:
  dashboard:
    max-atm-list-count: 100

dashboard:
  api-urls:
    - "http://localhost:9208/sampatti/get-transaction-last-updated-date-time"
    - "http://localhost:9200/dashboard-portal/currentUptime"
    - "http://localhost:9200/dashboard-portal/numberOfCes"
    - "http://localhost:9200/cm-dashboard/counts"
    - "http://localhost:9200/cm-dashboard/getCmSynopsisDetails"
    - "http://localhost:9200/dashboard-portal/activeMachine"
    - "http://localhost:9202/task/list/ce"
    - "http://localhost:9202/ticket/get_trans_target"

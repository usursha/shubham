version: '3.8'

services:
  redis:
    image: redis:latest
    container_name: ops360_redis
    ports:
      - "6379:6379"
    volumes:
      - redis-data:/data
    networks:
      - redis-network

volumes:
  redis-data:

networks:
  redis-network:
    driver: bridge